name: Install Nala on Debian
hosts: localhost
become: yes
tasks:
  - name: update apt cache
    apt:
      update_cache: yes
      
  - name: install nala
    apt:
      name: nala
      state: present